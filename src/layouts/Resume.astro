<div class="resume-container">
  <div class="resume">
    <slot />
  </div>
</div>

<style is:global>
  .resume-container {
    max-width: 62.5rem;
    padding: 6.25rem;
    margin: 1rem;
    background-color: white;
  }

  .resume {
    --ratio: 1.5;
    --s-3: calc(var(--s-2) / var(--ratio));
    --s-2: calc(var(--s-1) / var(--ratio));
    --s-1: calc(var(--s0) / var(--ratio));
    --s0: 1rem;
    --s1: calc(var(--s0) * var(--ratio));
    --s2: calc(var(--s1) * var(--ratio));

    --default-block-spacing: var(--s0);
  }

  .resume h1 {
    font-size: var(--s2);
  }

  .resume h1 + ul {
    --list-item-spacing: var(--s-3);

    list-style: none;
    padding-inline-start: 0;

    display: flex;
    gap: var(--list-item-spacing);
  }
  .resume h1 + ul li:not(:last-child):after {
    content: "·";
    margin-inline-start: var(--list-item-spacing);
  }

  .resume a {
    text-decoration: none;
    color: inherit;
  }

  .resume h2 {
    text-transform: uppercase;
    font-size: var(--s0);
    font-weight: 900;
  }

  .resume h2,
  .resume .columns {
    --h2-spacing: var(--default-block-spacing);

    position: relative;
    margin-block-start: calc(2 * var(--h2-spacing));
  }
  .resume .columns h2 {
    margin-block-start: 0;
  }
  .resume h2:before,
  .resume .columns:before {
    /* Divider */
    content: "";
    height: 1px;
    width: 100%;
    background-color: #000;
    position: absolute;
    inset-block-start: calc(-1 * var(--h2-spacing));
    inset-inline-start: 0;
  }
  .resume .columns h2:before {
    display: none;
  }

  .resume h3 {
    font-size: var(--s0);
    font-weight: 700;
    margin-block-start: var(--default-block-spacing);
  }

  .resume .competences ul {
    list-style: none;
    padding-inline-start: 0;
    display: grid;
    grid-template-rows: 1fr 1fr;
    grid-auto-flow: column;
    gap: var(--s-3);
  }

  .resume .experience-professionnelle h3 {
    margin-block-start: 0;
  }
  .resume .experience-professionnelle hr {
    display: none;
  }
  .resume .experience-professionnelle ul {
    padding-inline-start: var(--s1);
  }
  .resume .experience-professionnelle hr + ul {
    margin-block-start: var(--default-block-spacing);
    list-style: none;
    padding-inline-start: 0;
    display: flex;
    justify-content: space-between;
    font-weight: 700;
  }
  .resume .experience-professionnelle h3,
  .resume .experience-professionnelle p,
  .resume .experience-professionnelle li {
    margin-block-end: var(--s-3);
  }

  .resume .columns {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: calc(2 * var(--s-3));
  }
  .resume .columns .column:first-child {
    position: relative;
  }
  .resume .columns .column:first-child:after {
    content: "";
    width: 1px;
    height: calc(100% + var(--default-block-spacing));
    position: absolute;
    right: var(--s-3);
    top: calc(-1 * var(--default-block-spacing));
    background-color: #000;
  }
</style>
